
# Placeholder: we reference a prebuilt image in devcontainer.json for speed.
# To build locally, start from an NVIDIA ROS 2 base image or Ubuntu 22.04 and install ROS 2 + deps.
FROM ubuntu:22.04
RUN apt-get update && apt-get install -y curl git
